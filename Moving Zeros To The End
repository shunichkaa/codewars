def move_zeros(arr):
    insert_index = 0
    for num in arr:
        if num != 0:
            arr[insert_index] = num
            insert_index += 1
    for i in range(insert_index, len(arr)):
        arr[i] = 0
    return arr
